  on:
    push:
      branches:
        - master





  jobs:
    build:
      runs-on: ubuntu-latest

      steps:
        - uses: action/checkout@v3

        - name: "Login to Docker"
          run: docker login --username khaled8 --password dckr_pat_Fcr3JjjiYPKwxth_i33Q_T_-IN4
           
